/** @todo Log to server
 * ----------------------------------------------------------------------
 * - genserve start analogger --port 3036 --namespace analogger
 * - Set an URL to analogger (http://localhost:3036/analogger.server.js)
 * - analogger.server.js should be able to forward all of the logs to a file
 * => Have an NPM, that launches genserve?
 * => Make analogger do the remote work as well?: analogger-server
 * ----------------------------------------------------------------------
 *
 * @todo Fix setOptions
 *
 */
